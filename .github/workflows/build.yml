name: Build Android APK
on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
      - uses: android-actions/setup-android@v3
      - run: sdkmanager "platforms;android-35" "build-tools;35.0.0"
      - run: npm install -g cordova
      - run: npm install
      - run: cordova platform add android || true
      - run: cordova build android --release --buildConfig=build.json
      - run: |
          APK=$(find platforms/android -name "*release*" -o -name "*signed*" | grep "\.apk" | head -1)
          if [ -z "$APK" ]; then APK=$(find platforms/android -name "*.apk" -type f | head -1); fi
          cp "$APK" "mini-genius-release.apk"
      - uses: actions/upload-artifact@v4
        with:
          name: mini-genius-release
          path: mini-genius-release.apk
          retention-days: 90
